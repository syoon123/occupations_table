<!DOCTYPE html>

<html>

  <head>
    <title> {{ title }} </title>
  </head>

  <body>
    <table>
      {% for key, value in d.iteritems() %}
      <tr>
	<td> {{ key }} </td>
	<td> {{ value }} </td>
      </tr>
      {% endfor %}
    </table>
  </body>

</html>
